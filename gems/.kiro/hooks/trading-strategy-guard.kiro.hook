{
  "enabled": true,
  "name": "交易策略規範守衛",
  "description": "修改 src/scripts/ 或 agent/skills/ 檔案時，自動提醒遵循 workflow 規範（CHANGELOG 格式、commit 流程）和交易策略規範（策略文件同步更新）",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "src/scripts/**/*.js",
      "agent/skills/**/*.md"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "檔案已修改。請確認：\n1. 如果修改了 src/scripts/loops/ 或 scanner.js，是否需要同步更新 agent/skills/specs_trading/ 對應的策略文件？\n2. 如果這是一個功能變動，是否需要更新 BOT_CHANGELOG.md（格式：條目 ≤ 10 點、總字數 ≤ 300 字、只有一層條目）？\n3. 策略參數有變動時，是否已更新對應文件的參數速查表？\n請參照 agent/skills/workflow/SKILL.md 的觸發條件表格執行對應步驟。"
  }
}